
import React from 'react';
import Login from './Login';

// This is a simple redirect component to maintain backward compatibility
const AdminLogin = () => {
  return <Login />;
};

export default AdminLogin;
